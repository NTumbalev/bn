{% extends '::base.html.twig' %}

{% import "::paginatorWithRouteAndSlug.html.twig" as pager %}

{% block main %}
    {% include '::breadCrumbs.html.twig' with {'breadCrumbs':breadCrumbs} %}

    <section id="main">
        {% include '::headerImage.html.twig' with {'content':content} %}

        <div class="mainBody">
            <div class="wrapper">
                {% set haveSideBar = false %}
                {% if sideBar is defined and sideBar is not null and sideBar|length %}
                    {% set haveSideBar = true %}
                {% endif %}
                <div class="mainContent{% if haveSideBar == false %} full{% endif %}">
                    <h1 class="titleMain">{{ content.title }}</h1>

                    {% if content.description is defined and content.description is not null and content.description|length %}
                        <article class="article">
                            {{ content.description|raw }}
                        </article>
                    {% endif %}

                    <div class="boxes boxesCompanies">
                        {% for companyCategory in companiesCategories %}
                            <div class="box">
                                <a href="{{ path(companyCategory.route, companyCategory.routeParams()) }}" class="photo" style="background-image: url({% if companyCategory.translations[app.request.locale].image is defined and companyCategory.translations[app.request.locale].image is not null and companyCategory.translations[app.request.locale].image|length %}{% path companyCategory.translations[app.request.locale].image, 'index' %}{% else %}'/assets/images/no-img.jpg'{% endif %});"></a>

                                <div class="content">
                                    <p class="title">
                                        <a href="{{ path(companyCategory.route, companyCategory.routeParams()) }}">
                                            {{ companyCategory.title }}
                                        </a>
                                    </p>

                                    {% if companyCategory.simpleDescription is defined and companyCategory.simpleDescription is not null and companyCategory.simpleDescription|length %}
                                        <div class="text">
                                            <p>{{ companyCategory.simpleDescription }}</p>
                                        </div>
                                    {% endif %}
                                </div>
                            </div><!-- /.box -->
                        {% endfor %}
                    </div><!-- /.boxes -->

                    {% if companiesCategories is defined and companiesCategories|length %}
                        {{ pager.pager(companiesCategories, 'companies_categories_list') }}
                    {% endif %}
                </div><!-- /.mainContent -->

                {% if haveSideBar %}
                    <aside class="sidebar">
                        <p class="titleAside">{{ content.title }}</p>

                        <nav class="navAside">
                            {{ sideBar|raw }}
                        </nav><!-- /.navAside -->
                    </aside><!-- /.sidebar -->
                {% endif %}
            </div><!-- /.wrapper -->
        </div><!-- /.mainBody -->
    </section>
{% endblock %}
